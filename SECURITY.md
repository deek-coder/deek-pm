# 安全策略

## 支持版本

当前仅维护最新发布版本。`v0.1.x` 属于早期版本，安全修复可能伴随配置或数据迁移。

## 报告漏洞

请通过 GitHub 仓库的 **Security → Report a vulnerability** 私密报告，不要创建公开 Issue。

报告建议包含：

- 受影响版本和部署方式。
- 可复现步骤与影响范围。
- 是否涉及认证绕过、数据泄露、路径穿越、任意文件访问或密钥暴露。
- 在不包含真实用户数据的前提下提供日志或最小示例。

请不要测试不属于你的公开实例，也不要提交真实密码、JWT、S3 凭据、数据库连接串或备份文件。

## 部署责任

自部署者应启用 HTTPS，限制数据库和对象存储网络访问，使用随机独立的 `JWT_SECRET`、`DATA_ENCRYPTION_KEY` 与 `SETUP_TOKEN`，并定期验证 PostgreSQL 和资产存储备份可恢复。
